# Local MongoDB for development:  docker compose up -d
services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - grn_mongo:/data/db
volumes:
  grn_mongo:
